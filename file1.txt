#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
	clrscr();
	char a[6],b;
	printf("Enter the string");
	gets(a);
	scanf(%c,&b);
	n=strlen(a);
	for(i=0;i<=n;i++)
	{
		if(a[i]==b)
		{
			c++
		}
	}
	printf("no of times it appears %d",&c);
	p=c/n;
	printf("prob of character %d",&p);
}